import { Produit } from './model.produit';
export class ProduitItems {
    id:number;
    nom:string;
    prix: number;
    quantite: number;
    tBCCV: string;
    /* constructor( produit: Produit, prix: number, quantite: number, ) {
        this.produit = produit;
        this.prix = prix;
        this.quantite = quantite;
    } */
}